# Genopedia core intentionally has no third-party runtime dependencies.
# Optional integrations are declared in pyproject.toml:
#   python -m pip install -e .[bio]
#   python -m pip install -e .[ml]
#   python -m pip install -e .[dev]
